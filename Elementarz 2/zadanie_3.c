#include <stdio.h>

int nxt_digit[100];
int j=0;

int main()
{
    for(int i=0; i>; i+=2;)  //nadal nie wiem co wstawić w for
    {j++;
        nxt_digit[j] = 4 - 4/i
        printf("Pi = %d", nxt_digit[j];);
    }
}
